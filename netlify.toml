[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# The application is a single-page app: every path is served by index.html so
# that a deep link does not 404 on a hard refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
